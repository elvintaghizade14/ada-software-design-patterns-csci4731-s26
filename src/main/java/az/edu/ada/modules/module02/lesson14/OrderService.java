package az.edu.ada.modules.module02.lesson14;

public class OrderService {
    /*
    private PaymentGateway payment;
    private InventorySystem inventory;
    private EmailService email;
    private ShippingDepartment shipping;
    private LoyaltyService loyaltyService;

    public void placeOrder(Order order) {
        // The God Class Anti-Pattern
        payment.chargeCard(order);
        inventory.deductStock(order);
        email.sendReceipt(order);
        shipping.printLabel(order);
        loyaltyService.calculateBonus(order);
    }
    */
}
